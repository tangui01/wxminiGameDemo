using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ConfigBGAddin : MonoBehaviour
{
    [SerializeField]
    SpriteRenderer bgShow;

    // Start is called before the first frame update
    void Start()
    {
        //≥ı ºªØ±≥æ∞
        var id = 1;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
