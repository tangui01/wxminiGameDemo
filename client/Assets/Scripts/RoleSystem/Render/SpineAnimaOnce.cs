//using Spine.Unity;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpineAnimaOnce : MonoBehaviour
{
    //Spine.AnimationState.TrackEntryDelegate onComplete = null;
    // Start is called before the first frame update
    void Start()
    {
        //var db = GetComponent<SkeletonAnimation>();
        //var state = db.AnimationState;
        //onComplete = delegate
        //{
        //    db.AnimationState.Complete -= onComplete;

        //    Destroy(gameObject);
        //};

        //state.Complete += onComplete;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
